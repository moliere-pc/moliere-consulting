# Base de donnees (SQLite en local ; PostgreSQL en production)
# Local :
DATABASE_URL="file:./dev.db"
# Production Vercel (PostgreSQL) :
# DATABASE_URL="postgresql://user:password@host:5432/moliere?schema=public"

# Cle secrete des sessions administrateur (generer : openssl rand -base64 32)
AUTH_SECRET="a-remplacer-par-une-cle-aleatoire-de-32+-caracteres"

# Compte administrateur cree au premier seed (a changer apres connexion)
ADMIN_EMAIL="admin@moliereservice.com"
ADMIN_PASSWORD="ChangerMotDePasse123!"
